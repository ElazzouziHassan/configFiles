server:
  port: 8888


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ElazzouziHassan/configFiles.git
          clone-on-start: true
          default-label: main
  security:
    user:
      password: admin
      name: admin